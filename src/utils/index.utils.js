import { isString, isNumber, isDecimal, isBoolean } from "./common/dataType.js";
import { isEmpty } from "./common/isEmpty.js";

export default {
    // common functions
    common : {
        isEmpty,
        isString,
        isNumber,
        isDecimal,
        isBoolean
    }
}